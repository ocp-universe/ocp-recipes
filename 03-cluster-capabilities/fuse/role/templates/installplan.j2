# InstallPlan in case manual approval is needed
---
apiVersion: operators.coreos.com/v1alpha1
kind: InstallPlan
metadata:
  name: "{{ ocp_recipe_operator_install_plan_name }}"
  namespace: openshift-operators
spec:
  approved: true
